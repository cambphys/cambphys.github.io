{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bundle config set --local path /tmp/bundle && bundle install && bundle exec jekyll build --trace",
  "outputDirectory": "_site",
  "cleanUrls": true
}
